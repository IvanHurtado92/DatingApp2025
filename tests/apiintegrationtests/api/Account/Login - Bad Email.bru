meta {
  name: Login - Bad Email
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/account/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "sadsadf@test.com",
    "password": "Pa$$w0rd"
  }
}

tests {
  test("Should be able to login", function() {
    expect(res.getStatus()).to.equal(401);
  })
  const data = res.body;
  bru.setEnvVar("authToken", data.token)
}

settings {
  encodeUrl: true
  timeout: 0
}
